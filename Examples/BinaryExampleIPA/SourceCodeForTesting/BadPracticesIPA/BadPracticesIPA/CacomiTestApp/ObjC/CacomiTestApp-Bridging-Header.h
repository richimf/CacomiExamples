//
//  CacomiTestApp-Bridging-Header.h
//
//  Bridge file kept minimal; Objective-C fixtures are compiled into
//  the same target so the scanner can analyze them. Swift does not
//  need to call them, but the import line ensures the header is
//  picked up when SWIFT_OBJC_BRIDGING_HEADER is configured.
//

#import "LegacyCrypto.h"
